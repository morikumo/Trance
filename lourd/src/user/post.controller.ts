// user/post.controller.ts
import { Controller } from '@nestjs/common';

@Controller('user/:userId/posts')
export class PostController {
  // Implémentez ici les opérations CRUD pour les publications liées à un utilisateur
}
